#include <stdio.h>
int main(){
    int a,b;   //a>b
    printf("Enter the values of a and b:");
    scanf("%d %d",&a ,&b);
    //int q = a/b;
    //int r = a - (b*q);
    int r = a % b;
    printf("reminder is: %d",r);
    return 0;
}
