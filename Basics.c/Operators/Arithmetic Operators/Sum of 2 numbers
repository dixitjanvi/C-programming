#include <stdio.h>
int main(){
    int x,y,sum;
    printf("Enter the first number:",x);
    scanf("%d", & x);
    printf("Enter the second number:",y);
    scanf("%d", & y);
    sum = x + y ;
    printf("Sum of the number is: %d",sum);
    return 0;
}
